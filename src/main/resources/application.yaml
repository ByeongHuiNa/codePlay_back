server:
  port: 8000

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://10.107.0.248/codeplay
    username: codeplay
    password: 123456

mybatis:
  mapper-locations: classpath:mapper/*/*.xml
  type-aliases-package: com.codeplay.domain
  