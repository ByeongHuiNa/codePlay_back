<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codeplay.mapper.userInformation.DeptAssignMapper">
    <select id="findByUserNo" parameterType="Integer" resultType="Dept_AssignVo">
        select * from dept_assign where user_no =#{user_no}
    </select>
    
</mapper>

